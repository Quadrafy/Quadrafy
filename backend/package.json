{
  "name": "padelfy-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "API e servidor web da plataforma Padelfy",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --env-file=.env --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/app.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.56.0"
  }
}
