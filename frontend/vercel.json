{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "$BACKEND_HTTPS_URL/api/:path*"
    },
    {
      "source": "/uploads/:path*",
      "destination": "$BACKEND_HTTPS_URL/uploads/:path*"
    }
  ]
}
